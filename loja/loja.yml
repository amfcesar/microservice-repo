spring:
  datasource:
    password: root
    username: root
    url: 'jdbc:mysql://localhost:3306/loja?useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true&autoReconnect=true'
  jpa:
    hibernate:
      ddl-auto: update
